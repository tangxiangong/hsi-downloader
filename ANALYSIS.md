# YuShi 功能分析与优化建议

## 未实现的功能

### 核心引擎 (yushi-core)

5. **无 HTTP/FTP/磁力链接等多协议支持** — 目前只处理 HTTP/HTTPS。


### 桌面 GUI (yushi-app / src/)

8. **无拖放/剪贴板监控** — 不能拖拽 URL 到窗口或自动捕获剪贴板中的下载链接。

9. **无系统托盘交互** — 虽然引入了 `tray-icon` 依赖，但没有实际的托盘菜单/通知实现。

10. **无浏览器扩展集成** — 作为下载管理器，缺少浏览器插件接收下载请求的能力。

### CLI / TUI

11. **TUI 缺少历史和设置视图** — TUI 模式只有任务列表，不能查看历史记录或修改配置。

12. **TUI 退出时不保存队列状态** — 直接退出，未持久化当前队列。

---

## 已实现但有问题需要优化

### 性能问题

### 健壮性问题

### 设计问题

---
